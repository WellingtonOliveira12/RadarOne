# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
dist/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Credentials (NEVER commit)
ADMIN_CREDENTIALS.md
*CREDENTIALS*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Prisma
prisma/*.db
prisma/*.db-journal

# Playwright browsers (instalados no build)
pw-browsers/
worker/pw-browsers/

# Docker
docker-compose.override.yml

# OS
.DS_Store
Thumbs.db

# Auth Sessions (NEVER commit - contains cookies)
sessions/
worker/sessions/
*.base64.txt
*storageState*.json

# Environment & Secrets (AIOS)
.env.*.local
*.key
*.pem

# Dependencies (AIOS)
node_modules

# Build & Logs (AIOS)
logs/

# IDE & OS (AIOS)
.idea/
*.swp

# AIOS Framework (runtime — never commit)
.aios-core/
.aios/

# AI IDE Configurations (AIOS multi-IDE agents)
.antigravity/
.codex/
.cursor/
.gemini/
.github/agents/
AGENTS.md

# Claude Code local settings
.claude/settings.local.json

# Test artifacts (generated on every run)
frontend/test-results/
backend/test-results/
worker/test-results/
test-results/
playwright-report/
*/playwright-report/

# TypeScript build cache
*.tsbuildinfo

# Backup / temp files
*.bak
*.backup
*.orig
*.tmp
*.patch
.trigger-deploy

# Session/prompt files (ephemeral)
SESSION_STATUS.md
CHECKPOINT_SESSAO.md
PROMPT_PROXIMA_SESSAO*.md
